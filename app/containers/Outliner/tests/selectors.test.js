// import { fromJS } from 'immutable';
// import { makeSelectOutlinerDomain } from '../selectors';

// const selector = makeSelectOutlinerDomain();

describe('makeSelectOutlinerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
